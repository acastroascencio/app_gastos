name: app_gastos
description: "Aplicativo Alquiler V1 - MCASTRO"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_core: ^2.24.2
  cloud_firestore: ^4.14.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5
  intl: ^0.19.0 

flutter:
  uses-material-design: true
  assets:
    - assets/logo_perro.gif